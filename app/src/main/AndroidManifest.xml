<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".IdleClickerActivity"
            android:exported="false" />
        <!-- Main menu launches first -->
        <activity
            android:name=".MainMenuActivity1"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Card Match Game -->
        <activity android:name=".MainActivity" />
        <activity android:name=".SnakeGameActivity" />
        <activity android:name=".TicTacToeActivity" />
        <activity android:name=".BrickBreakerActivity" />
        <activity android:name=".RockPaperScissorsActivity" />
        <activity android:name=".SudokuActivity" />
        <activity android:name=".RunnerActivity" />
    </application>

</manifest>